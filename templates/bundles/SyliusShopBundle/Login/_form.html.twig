{% import '@SyliusUi/Macro/messages.html.twig' as messages %}

{% if last_error %}
    {{ messages.error(last_error.messageKey|trans(last_error.messageData, 'security')) }}
{% endif %}

{{ form_widget(form._username, {'attr': {'class': 'input'}}) }}
{{ form_widget(form._password, {'attr': {'class': 'input'}}) }}
{#{{ form_widget(form._remember_me, sylius_test_form_attribute('remember-me')) }}#}
