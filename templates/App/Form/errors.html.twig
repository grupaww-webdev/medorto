{% extends '@SyliusUi/Form/theme.html.twig' %}

{%- block form_errors -%}
    <div class="flex flex--column error mb-4">
        {%- for error in errors -%}
            <p class="flex flex--between">
                {{ error.message }}
            </p>
        {%- endfor -%}
    </div>
{%- endblock form_errors -%}

